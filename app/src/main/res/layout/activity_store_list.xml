<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".StoreListActivity">

    <TextView
        android:id="@+id/textStore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/open_sans"
        android:layout_margin="10dp"
        android:text="Stores near me"
        android:textSize="24dp"
        android:drawableStart="@drawable/baseline_store_mall_directory_black_24dp"
        android:drawablePadding="5dp"/>

    <EditText
        android:id="@+id/searchStore"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginHorizontal="10dp"
        android:layout_below="@+id/textStore"
        android:background="@drawable/ic_border"
        android:drawableRight="@drawable/baseline_search_black_24dp"
        android:padding="10dp"
        android:hint="Search nearby store"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/storeList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/searchStore"
        android:layout_marginTop="20dp" />

</RelativeLayout>